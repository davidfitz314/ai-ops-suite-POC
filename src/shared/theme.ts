export const theme = {
    colors: {
      // Base surfaces
      background: "#f8fafc",   // app background
      surface: "#ffffff",      // cards / panels
      surfaceSubtle: "#f1f5f9", // soft contrast (use sparingly)
  
      border: "#e5e7eb",
  
      // Text
      textPrimary: "#111827",
      textSecondary: "#6b7280",
      textMuted: "#9ca3af",
  
      // Brand / actions
      primary: "#2563eb",
      primaryHover: "#1d4ed8",
  
      // Semantic states
      success: "#10b981",
      warning: "#f59e0b",
      danger: "#ef4444",
  
      // Message system (NEW — replaces green chat look)
      messageInbound: "#f1f5f9",   // light neutral (external input)
      messageOutbound: "#e2e8f0",  // slightly stronger neutral (system response)
  
      // Optional accent usage (future)
      accentSoft: "#dbeafe", // subtle blue highlight (selection / focus)
    },
  };